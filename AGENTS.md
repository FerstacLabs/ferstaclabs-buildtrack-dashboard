# BuildTrack Agent Rules

- UI language must be Azerbaijani.
- Match the design reference images in docs/design-reference.
- Do not introduce backend/auth unless explicitly asked.
- Keep data service abstraction so local data can later be replaced by API.
- Always run npm run build before finishing.
- Keep pages clean and not overcrowded.